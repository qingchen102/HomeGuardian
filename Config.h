// include/Config.h
// ==========================================
// HomeGuardian·双子星 —— 全局配置总控面板
// 团队规定：所有网络、密码、引脚号的修改，只能在这个文件里进行！
// ==========================================

// 防止头文件被重复引用的保护宏（C++ 规范必写）
#ifndef CONFIG_H
#define CONFIG_H

/* ==========================================
 * 1. 网络与云端大脑配置
 * ========================================== */
// 替换为你测试环境的 Wi-Fi 名称和密码（建议用手机开2.4G热点，最稳定）
#define WIFI_SSID       "Your_WiFi_Name"        
#define WIFI_PASSWORD   "Your_WiFi_Password"    

// 大模型 API 配置（这里预留火山引擎豆包的 Key，之后申请了再填）
#define LLM_API_KEY     "your_doubao_api_key_here"   

// MQTT 云端消息流转服务器（用于宠物和小程序/沙发节点通信）
// 这里预填的是 EMQX 免费提供的公共测试服务器，可以直接用
#define MQTT_SERVER     "broker.emqx.io"        
#define MQTT_PORT       1883                    

/* ==========================================
 * 2. 硬件引脚配置 (引脚图映射)
 * ========================================== */
// 假设你们使用 MX1508 或 L298N 驱动模块控制小车的左右轮子
// 注意：以下引脚号只是占位符，等你们实际用杜邦线连到 ESP32-C5 上的哪根针，就改成对应的数字
#define PIN_MOTOR_LEFT_F    4   // 左轮前进引脚
#define PIN_MOTOR_LEFT_B    5   // 左轮后退引脚
#define PIN_MOTOR_RIGHT_F   6   // 右轮前进引脚
#define PIN_MOTOR_RIGHT_B   7   // 右轮后退引脚

/* ==========================================
 * 3. FreeRTOS 系统多线程调度配置
 * ========================================== */
// 给 AI 聊天和网络通信分配足够的内存（8KB），防止大模型返回字数太多导致内存溢出崩溃
#define TASK_STACK_SIZE 8192 

// 定义 ESP32 芯片的两个核心的名字，方便后续代码阅读
#define CORE_0          0    // 核心0：干体力活（负责底盘运动、监听报警信号）
#define CORE_1          1    // 核心1：干脑力活（负责连Wi-Fi、调用大模型聊天）

#endif // CONFIG_H 结束